// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/go .
{
	"name": "Go",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	// "image": "mcr.microsoft.com/devcontainers/go:2-1.25-trixie"
	"workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",

	"build": {
		"dockerfile": "Containerfile"
	},
	// Features to add to the dev container. More info: https://containers.dev/features.
	"features": {
    	"ghcr.io/devcontainers/features/docker-in-docker:2": {
			"moby": false
		},
		"ghcr.io/devcontainers/features/node:1": {
			"version": "lts"
		}
	},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [5173, 8787],
	"portsAttributes": {
		"5173": { "label": "Vite dev server" },
		"8787": { "label": "Go server" }
	},

	// Use 'postCreateCommand' to run commands after the container is created.
	"postCreateCommand": ".devcontainer/post_create.sh",

	// Configure tool-specific properties.
	"customizations": {
		"vscode": {
			"extensions": [
				"golang.go",
				"qwtel.sqlite-viewer",
				"vue.volar",
				"dbaeumer.vscode-eslint",
				"EditorConfig.EditorConfig",
				"oxc.oxc-vscode",
				"esbenp.prettier-vscode",
				"-vscode.typescript-language-features" // conflicts with vue.volar https://stackoverflow.com/a/73710755
			],
			"settings": {
				"ts.tsdk.path": "web/app/node_modules/typescript/lib",
				"eslint.workingDirectories": [
					{ "directory": "web/app", "changeProcessCWD": true }
				],
				"prettier.configPath": "web/app/.prettierrc.json",
				"oxc.configPath": "web/app/.oxlintrc.json"
			}
		}
	}

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
